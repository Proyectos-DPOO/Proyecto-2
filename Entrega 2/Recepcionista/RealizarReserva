package Recepcionista;

import java.awt.FlowLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JButton;
import javax.swing.JFrame;


public class RealizarReserva extends JFrame implements ActionListener{
		public RealizarReserva() {
			super("Realizar Reserva");
			
			setLayout(new FlowLayout());
			setSize(400,400);
			setLocationRelativeTo(null);
			CamaButton = new JButton("Cama");
			add(CamaButton);		
			
			setLayout(new FlowLayout());
			setSize(400,400);
			setLocationRelativeTo(null);
			HabitacionButton = new JButton("Habitación");
			add(HabitacionButton);
			
		}	
		@Override
		public void actionPerformed(ActionEvent e) {
			
		}
		private JButton CamaButton;
		private JButton HabitacionButton;
		
}

